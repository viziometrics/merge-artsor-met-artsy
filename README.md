# merge artsor met artsy

## merge.py
has the code where all three sources are merged into one big file.

## Histogram
`histo.py` has the code which will generate a histogram of the years of art work that exists.